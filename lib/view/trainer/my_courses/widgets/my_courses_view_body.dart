import 'package:flutter/material.dart';

class MyCoursesViewBody extends StatelessWidget {
  const MyCoursesViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
