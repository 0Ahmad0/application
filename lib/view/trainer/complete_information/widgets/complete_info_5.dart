import 'package:flutter/material.dart';

class CompleteInfo5 extends StatelessWidget {
  const CompleteInfo5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
